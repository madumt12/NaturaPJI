<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Produtos</title>
    <style>
        body {
            background-color: #f2f2f2;
            text-align: center;
        }
        table {
            width: 100%;
            margin: auto;
            border-collapse: collapse;
        }
        td {
            padding: 20px;
            vertical-align: top;
            text-align: center;
        }
        img {
            width: 200px;
            height: auto;
        }
    </style>
</head>
<body>
    <table>
        <tr>
            <% for (let i = 0; i < tabela.rowCount; i++) { %>
                <td>
                    <form action="/comprar" method="post">
                        <input type="text" name="txtCodigo" value="<%= tabela.rows[i].codigo %>" hidden />
                        <input type="text" name="txtDescricao" value="<%= tabela.rows[i].descricao %>" hidden />
                        <input type="text" name="txtPreco" value="<%= tabela.rows[i].preco %>" hidden />
                        <img src="/image/<%= tabela.rows[i].imagem %>" alt="<%= tabela.rows[i].descricao %>" /> <br/>
                        <font size="6"> <%= tabela.rows[i].descricao %> </font> <br/>
                        <font size="5"> Preço R$ <%= tabela.rows[i].preco %> </font> <br/>
                        Quantidade: <input type="text" name="txtQtde" value="1" /> <br/><br>
                        <input type="submit" name="b1" value="Comprar" style="padding: 10px; font-size: 16px;color: black;background-color: #FF6A13;" />
                    </form>
                </td>

                <% if ((i + 1) % 3 === 0) { %> <!-- Fecha a linha após cada 3 produtos -->
                    </tr><tr>
                <% } %>
            <% } %>
        </tr>
    </table>
</body>
</html>
